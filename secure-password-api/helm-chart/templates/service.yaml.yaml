apiVersion: v1
kind: Service
metadata:
  name: {{ include "secure-password-api.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "secure-password-api.fullname" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8000
